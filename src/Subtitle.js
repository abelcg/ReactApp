import React from 'react';

const Subtitle= () => {
    
    return (
        <h2 className="text-center text-secondary">Add a character with a name and a job to the table.</h2>
    );
};

export default Subtitle;